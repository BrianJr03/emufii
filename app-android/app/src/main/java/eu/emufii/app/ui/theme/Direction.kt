package eu.emufii.app.ui.theme

/**
 * Anchor for the direction contract, so code can name it.
 *
 * The contract itself — thesis, own-world, story, first viewport, form, finish —
 * is in docs/decisions/direction-visuelle.md § Le contrat de direction —
 * « HOME MENU ». The system it produced is in DESIGN.md.
 */
internal object Direction
